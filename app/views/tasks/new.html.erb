<h1>New Task</h1>

<%= form_for(@task) do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>

  <%= f.label :details %>
  <%= f.text_field :details %>
  <p><%= f.check_box :completed %> Completed</p>
  <%= f.submit %>

<% end %>
